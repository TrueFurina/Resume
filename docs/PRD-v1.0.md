# PRD V1.0

> 物理学科智能排课选课智能体系统（Solo 版）
> 版本：V1.0 ｜ 日期：2026-08-16 ｜ 状态：定稿（基于 MVP 评审意见迭代）

---

## 1. 产品概述

### 1.1 一句话定位

面向高校物理实验教学场景的 **四大模块智能体系统**：排课、选课、调课、辅导，用 AI 对话降低操作门槛、用规则引擎保证排课结果可靠。

### 1.2 项目背景

- 物理实验教学涉及多专业、多教师、多实验室的复杂排课约束
- 教务系统管不了实验排课（论文验证："实验课很少进排课系统，靠人工协调"）
- 教师调课引发多班多教室连锁冲突，人工协调效率低
- 学生选课受先修课/学分约束，看不懂培养方案

### 1.3 产品目标

解决"排课协调难、调课冲突多、选课咨询难、实验辅导缺"四大问题，为教师、学生、管理员提供 AI 辅助的完整闭环。

---

## 2. 用户与角色

| 角色 | 使用模块 | 核心场景 |
|------|---------|---------|
| **实验教师** | 排课、调课、辅导 | 查看我的课程、发起调课、查看候选时段 |
| **学生** | 选课、辅导 | 选课资格咨询、实验原理/数据处理辅导 |
| **教学管理员** | 排课、审批 | 审批调课申请、查看冲突 |

**身份方案**：可视化选择（替代账号密码登录）——系统知道用户是谁，展示其名下课程，演示友好、满足评审"系统应知道用户是谁"要求。

---

## 3. 四大核心模块需求

### 3.1 排课模块（冲突筛选 + 候选方案）

| 要素 | 内容 |
|------|------|
| 用户 | 教师、管理员 |
| 功能 | 身份选择 → 我的课程列表（可调/不可调标注）→ 冲突筛选 → 原因标注 → 候选方案 |
| 冲突检查 | 教室占用 / 学生课表 / 教师自身时间（三重） |
| 输出 | 可用时段（绿）+ 被排除时段（灰，附原因）+ AI 候选方案 |
| 验收 | 每个不可选时段有明确原因；候选方案 ≥2 个 |

### 3.2 选课模块（LLM 咨询）

| 要素 | 内容 |
|------|------|
| 用户 | 学生 |
| 功能 | 输入自然语言问题（选课资格/先修课/学分），LLM 基于学生课表回答 |
| 输出 | 结论明确 + 原因解释 + 可操作建议 |
| 验收 | 回答包含结论与建议；信息不足时说明并建议咨询管理员 |

### 3.3 调课模块（核心闭环）

| 要素 | 内容 |
|------|------|
| 用户 | 教师（发起）、管理员（审批） |
| 功能 | 模板表单（选课+选周次）→ 冲突筛选 → 提交申请 → 管理员审批 → 反馈 |
| 交互 | 火车票改签式：先看"我的票"，再选"改签时间" |
| 解析 | 规则解析优先 + LLM 大白话解析 + 多轮追问 |
| 验收 | 调课全流程闭环可演示；审批状态流转正确 |

### 3.4 辅导模块（LLM 咨询）

| 要素 | 内容 |
|------|------|
| 用户 | 学生 |
| 功能 | 实验原理、操作流程、数据处理、误差分析、报告规范问答 |
| 输出 | 步骤清晰、公式正确、引用实验规范 |
| 验收 | 回答基于实验规范，不编造 |

---

## 4. 核心流程

### 4.1 排课调课流程

```
身份选择（教师卡片）→ 我的课程（可调/不可调）
  → 模板表单（选课程 + 目标周次）→ 冲突筛选
  → 结果（可用时段 + 被排除原因 + AI候选方案）
  → 提交申请 → 管理员审批（同意/驳回）→ 反馈教师
```

### 4.2 选课/辅导流程

```
选择模块（选课咨询/实验辅导）→ 输入问题 → LLM 基于上下文回答 → 展示
```

### 4.3 多轮追问（调课信息不全时）

```
输入"我想调个课" → 系统追问（教师？哪门课？调到哪周？）→ 用户补充 → 继续处理
```

---

## 5. 功能需求明细

### F1 教师身份选择
- 展示全部教师卡片（从实验安排表提取）
- 点击进入该教师的调课工作台

### F2 我的课程列表
- 展示教师名下全部课程卡片
- 标注【可调/不可调】+ 不可调原因
- 时间友好显示（567 → 13:30-15:45）

### F3 模板表单调课
- 选课程 → 下拉选目标周次 → 自动查询
- 减少自由文本输入（评审建议）

### F4 冲突筛选 + 原因标注
- 三重检查：教室占用/学生课表/教师时间
- 每个被排除时段附原因（信息透传，评审核心要求）

### F5 自然语言调课（LLM）
- 大白话解析："我是吴琳，下周三有事，把实验课往后挪一周"
- 规则解析失败时降级到 LLM

### F6 管理员审批流
- 教师提交申请（待审批）→ 管理员同意/驳回 → 反馈

### F7 选课咨询（LLM）
- 选课资格、先修课、学分问题自然语言问答

### F8 实验辅导（LLM）
- 实验原理、数据处理、误差分析、报告规范问答

### F9 AI 候选方案
- LLM 从可用时段生成 2-3 个候选 + 推荐理由
- LLM 不可用时规则降级

---

## 6. 范围边界

### 6.1 本期做

- 四大模块最小闭环（排课/选课/调课/辅导）
- LLM 增强（律动基元接入）
- 管理员审批

### 6.2 本期不做

| 不做 | 原因 |
|------|------|
| 账号密码登录 | 可视化身份选择已满足需求 |
| 排课自动生成算法 | 当前做冲突筛选+候选推荐 |
| 移动端适配 | 时间限制 |
| 教务系统对接 | 依赖学校接口 |
| 申请单持久化 | 内存存储满足演示（后续 SQLite） |

---

## 7. 验收标准

| 场景 | 通过标准 |
|------|---------|
| 身份选择 | 展示全部教师，点击进入对应工作台 |
| 课程列表 | 展示教师全部课程，标注可调/不可调 |
| 调课筛选 | 可用时段 + 被排除时段含原因 |
| 大白话调课 | LLM 正确解析目标周次 |
| 信息缺失 | 系统追问补齐信息 |
| 提交/审批 | 状态流转 pending → approved/rejected |
| 选课咨询 | LLM 回答含结论与建议 |
| 实验辅导 | LLM 回答实验问题 |
| AI 候选 | ≥2 个候选方案（含推荐理由） |

---

## 8. 版本规划

| 版本 | 时间 | 内容 |
|------|------|------|
| v0.1~v0.6 | 7/20-8/1 | 需求分析、最小闭环 |
| v1.0 | 8/8 | 四大模块完整闭环 + LLM 增强 + 评审意见落实 |
| v2.0+ | 后续 | 申请单持久化、多学生课表、排课自动生成、知识库 RAG |

---

## 9. 风险

| 风险 | 影响 | 应对 |
|------|------|------|
| LLM 服务不稳定（503） | 解析/咨询失败 | 规则引擎兜底 + 静默降级 |
| 与组长协作脱节 | 整体系统不完整 | 已确认 Solo 提交，独立交付 |
| 数据仅闽江大学 | 推广性存疑 | 数据驱动，各校上传总表 |
| 时间紧张 | 交付物不完整 | 聚焦核心闭环，文档与产品一致 |
